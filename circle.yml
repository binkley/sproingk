dependencies:
   override: 
     - mvn install -DskipTests 
test:
  post:
    - mkdir -p $CIRCLE_TEST_REPORTS/junit/
    - find . -type f -regex ".*/target/(surefire|failsafe)-reports/.*xml" -exec cp {} $CIRCLE_TEST_REPORTS/junit/ \;
